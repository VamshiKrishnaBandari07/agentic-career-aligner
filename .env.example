# Works out of the box — no API key needed
MATCH_PROVIDER=free
OPENAI_API_KEY=

# Optional: AI-powered matching (requires OpenAI billing)
# MATCH_PROVIDER=openai
# OPENAI_API_KEY=sk-your-key-here

# Optional: disable browser UI (API docs only at /)
# SERVE_UI=false

MAX_PDF_MB=10
